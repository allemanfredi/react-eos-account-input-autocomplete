{
  "name": "react-eos-account-input-autocomplete",
  "version": "0.1.2",
  "private": false,
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "dependencies": {
    "async-mutex": "^0.2.2",
    "eosjs": "^20.0.3",
    "node-fetch": "^2.6.7",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "format": "prettier --config ./.prettierrc --write  \"./src/**/*.+(js|json|jsx)\"",
    "publish:npm": "rm -rf dist && mkdir dist &&  babel src/components -d dist --copy-files && npm publish"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "license": "MIT",
  "keywords": [
    "eos",
    "react",
    "blockchain"
  ],
  "author": "alle.manfredi@gmail.com <alle.manfredi@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/allemanfredi/react-eos-account-input-autocomplete"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/preset-react": "^7.9.4",
    "prettier": "^2.0.5"
  }
}
